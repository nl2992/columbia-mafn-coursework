#!/bin/zsh
set -u

script_dir="${0:A:h}"
repo_root="${script_dir}/../../.."
cd "${repo_root}" || exit 1
mkdir -p .rag/logs

exec /usr/bin/env python3 scripts/run_rag.py --port 8765 --open >> .rag/logs/course-archive-app.log 2>&1
